package me.glomdom.gantry.content.item.forms

import org.bukkit.Material
import org.bukkit.inventory.ItemStack

class RoughBaseForm(stack: ItemStack) : BaseForm(stack) {
    fun onBreak(mat: Material): Unit {

    }
}